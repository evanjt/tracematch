[package]
name = "tracematch-python"
version = "0.2.1"
edition = "2024"
publish = false

[lib]
name = "tracematch_python"
crate-type = ["cdylib"]

[dependencies]
tracematch = { path = ".." }
pyo3 = { version = "0.25", features = ["extension-module"] }
